---
import "../styles/global.css";
---

<!doctype html>
<html lang="fr" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content="Analyse des données de pingouins - Visualisation interactive" />
    <title class="text-white">Penguin Analytics — Astro Dashboard</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
  </head>
  
  <body class="bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-100 min-h-screen">
    <header class="sticky top-0 z-50 backdrop-blur-md bg-white/80 border-b border-white/20 shadow-sm">
      <div class="max-w-6xl mx-auto px-6 py-4">
        <div class="flex items-center justify-between">
          <div class="flex items-center space-x-3">
            <div>
              <h1 class="text-xl font-bold text-white">Penguin Analytics</h1>
              <p class="text-xs text-white">Analyse morphométrique des espèces</p>
            </div>
          </div>

          <!-- Navigation principale -->
          <nav class="flex items-center space-x-2">
            <div class="flex bg-white/60 backdrop-blur-sm rounded-xl p-1.5 shadow-inner border border-white/40">
              <a href="/species/Adelie" 
                 class="nav-link group px-4 py-2 rounded-lg text-sm font-medium transition-all duration-200 hover:bg-red-100 hover:text-red-700 active:scale-95">
                <span class="flex items-center space-x-2">
                  <div class="w-3 h-3 bg-red-400 rounded-full group-hover:scale-110 transition-transform"></div>
                  <span>Adelie</span>
                </span>
              </a>
              
              <a href="/species/Gentoo" 
                 class="nav-link group px-4 py-2 rounded-lg text-sm font-medium transition-all duration-200 hover:bg-teal-100 hover:text-teal-700 active:scale-95">
                <span class="flex items-center space-x-2">
                  <div class="w-3 h-3 bg-teal-400 rounded-full group-hover:scale-110 transition-transform"></div>
                  <span>Gentoo</span>
                </span>
              </a>
              
              <a href="/species/Chinstrap" 
                 class="nav-link group px-4 py-2 rounded-lg text-sm font-medium transition-all duration-200 hover:bg-blue-100 hover:text-blue-700 active:scale-95">
                <span class="flex items-center space-x-2">
                  <div class="w-3 h-3 bg-blue-400 rounded-full group-hover:scale-110 transition-transform"></div>
                  <span>Chinstrap</span>
                </span>
              </a>
            </div>

            <!-- Menu TP -->
            <div class="ml-4 flex space-x-2">
              <a href="/test-iframe" 
                 class="tp-link px-4 py-2 bg-gradient-to-r from-purple-500 to-pink-500 text-white text-sm font-medium rounded-lg shadow-md hover:shadow-lg transform hover:-translate-y-0.5 transition-all duration-200 active:scale-95">
                <span class="flex items-center space-x-1.5">
                  <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                    <path d="M4 3a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V5a2 2 0 00-2-2H4zm12 12H4l4-8 3 6 2-4 3 6z"></path>
                  </svg>
                  <span>TP2 — IFrame</span>
                </span>
              </a>
              
              <a href="/anchors" 
                 class="tp-link px-4 py-2 bg-gradient-to-r from-emerald-500 to-cyan-500 text-white text-sm font-medium rounded-lg shadow-md hover:shadow-lg transform hover:-translate-y-0.5 transition-all duration-200 active:scale-95">
                <span class="flex items-center space-x-1.5">
                  <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M12.586 4.586a2 2 0 112.828 2.828l-3 3a2 2 0 01-2.828 0 1 1 0 00-1.414 1.414 4 4 0 005.656 0l3-3a4 4 0 00-5.656-5.656l-1.5 1.5a1 1 0 101.414 1.414l1.5-1.5zm-5 5a2 2 0 012.828 0 1 1 0 101.414-1.414 4 4 0 00-5.656 0l-3 3a4 4 0 105.656 5.656l1.5-1.5a1 1 0 10-1.414-1.414l-1.5 1.5a2 2 0 11-2.828-2.828l3-3z" clip-rule="evenodd"></path>
                  </svg>
                  <span>Ancres & Slider</span>
                </span>
              </a>
            </div>
          </div>
        </div>
      </div>
    </header>

    <!-- Contenu principal -->
    <main class="max-w-6xl mx-auto px-6 py-8">
      <div class="animate-fadeIn">
        <slot />
      </div>
    </main>

    <!-- Footer -->
    <footer class="mt-16 bg-white/40 backdrop-blur-sm border-t border-white/30">
      <div class="max-w-6xl mx-auto px-6 py-8">
        <div class="flex flex-col md:flex-row items-center justify-between space-y-4 md:space-y-0">
          <div class="flex items-center space-x-3 text-sm text-gray-600">
            <span>Propulsé par</span>
            <div class="flex items-center space-x-2">
              <svg class="w-5 h-5 text-orange-500" viewBox="0 0 24 24" fill="currentColor">
                <path d="M12 0L1.608 6v12L12 24l10.392-6V6L12 0zm-1 13.89V2.984L4 7.5v8.39l7-1.99zm2 0l7 1.99V7.5l-7-4.516v11.906z"/>
              </svg>
              <span class="font-semibold">Astro</span>
            </div>
            <span>&</span>
            <div class="flex items-center space-x-2">
              <svg class="w-5 h-5 text-blue-500" viewBox="0 0 24 24" fill="currentColor">
                <path d="M12 0C5.373 0 0 5.373 0 12s5.373 12 12 12 12-5.373 12-12S18.627 0 12 0zm5.894 16.894l-1.789-1.789L12 19.211l-4.105-4.106-1.789 1.789L12 22.789l5.894-5.895z"/>
              </svg>
              <span class="font-semibold">Observable Plot</span>
            </div>
          </div>
          
          <div class="text-xs text-gray-400">
            Dashboard interactif — Données Palmer Penguins
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>

<style>
  * {
    font-family: 'Inter', system-ui, sans-serif;
  }
  
  html,
  body {
    margin: 0;
    width: 100%;
    min-height: 100vh;
    font-feature-settings: "tnum", "kern";
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fadeIn {
    animation: fadeIn 0.6s ease-out;
  }

  .nav-link {
    position: relative;
    color: #64748b;
  }

  .nav-link:hover {
    transform: translateY(-1px);
  }

  .nav-link.active {
    background: linear-gradient(135deg, #3b82f6, #1d4ed8);
    color: white;
    shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
  }

  .tp-link:hover {
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
  }

  html {
    scroll-behavior: smooth;
  }

  @media (max-width: 768px) {
    .max-w-6xl {
      padding-left: 1rem;
      padding-right: 1rem;
    }
    
    .flex.items-center.justify-between {
      flex-direction: column;
      align-items: flex-start;
      gap: 1rem;
    }
    
    .flex.space-x-2 {
      flex-wrap: wrap;
      gap: 0.5rem;
    }
  }


  @media (prefers-color-scheme: dark) {
    body {
      background: linear-gradient(135deg, #0f172a 0%, #1e293b 50%, #334155 100%);
    }
    
    header {
      background: rgba(15, 23, 42, 0.8);
      border-color: rgba(255, 255, 255, 0.1);
    }
  }
</style>